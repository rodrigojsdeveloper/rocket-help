import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "#FFFFFF",
    fontWeight: "600",
    fontSize: 20,
    lineHeight: 24,
    marginBottom: 15,
  },
  login: {
    marginTop: 77,
  },
});

export { styles };
